# Билеты

Порядок: T1 → (T2 ∥ T3) → T4 → T5 → T6. Контракты интерфейсов — SPEC.md §7, DOM — §8.

## T1 — Каркас и интерфейс
**Файлы:** `index.html`, `css/style.css`, `js/ui.js` (полная), `js/audio.js`/`js/bosses.js`/`js/fx.js`/`js/game.js` (заглушки с сигнатурами по SPEC §7).
**Даёт:** страница открывается с `file://`, анимированное неоновое меню, все экраны (меню/пауза/гейм-овер/управление), HUD, кнопки переключают экраны, глитч-заголовок, сканлайны/виньетка.
**Проверка:** `node --check` на всех js; все id из SPEC §8 присутствуют; скрипты в порядке §7.

## T2 — Звуковой движок
**Файлы:** `js/audio.js` (полная реализация по SPEC §5, §7).
**Даёт:** синтезированная музыка меню/игры/босса + 12 SFX, mute с сохранением.
**Проверка:** `node --check`; вызовы до ensure() не падают.

## T3 — Система боссов
**Файлы:** `js/bosses.js` (полная реализация по SPEC §4, §7).
**Даёт:** класс BossFight: 3 типа атак (лазер, файрвол, рывок), заряды, HP, отрисовка, события.
**Проверка:** `node --check`; API соответствует контракту буква в букву.

## T4 — Ядро игры
**Файлы:** `js/game.js` (полная реализация по SPEC §2, §3, §4-интеграция, §7).
**Даёт:** змейка с плавным движением, буфер ввода (стрелки/WASD/свайпы), еда и бонусы, уровни и скорость, боссы каждые 3 уровня, смерть, рекорд, пауза, привязка звука и UI.
**Проверка:** `node --check`; интеграция всех модулей по контрактам.

## T5 — Эффекты и шлифовка
**Файлы:** `js/fx.js` (полная), точечные правки `js/game.js`/`css/style.css`/`index.html`, `README.md`.
**Даёт:** частицы, тряска, глитч, вспышки, финальный тюнинг сложности, README с описанием запуска.
**Проверка:** `node --check`; визуальный прогон.

## T6 — Браузерное тестирование
Основной агент: загрузка в браузере, скриншоты, консоль без ошибок, симуляция клавиш, багфиксы.
