# NEON://SNAKE — Спецификация

Киберпанк-змейка для браузера. Один запуск `index.html` — без сборки, без CDN, без сети (работает с `file://`).

## 1. Обзор

- Жанр: классическая змейка на сетке с уровнями, боссами и синтезированным звуком.
- Платформа: любой современный браузер (Chrome/Edge/Firefox). Десктоп — основной, тач — поддерживается свайпами.
- Язык интерфейса: русский. Комментарии/идентификаторы кода: английские.
- Технологии: HTML5 Canvas 2D, Web Audio API, vanilla JS. **Без** ES-модулей (только классические `<script>`), **без** внешних шрифтов/библиотек — офлайн-first.

## 2. Управление (удобство — приоритет)

| Действие | Клавиши | Тач |
|---|---|---|
| Направление | Стрелки **и** WASD (одновременно) | Свайпы в 4 стороны (порог 24px) |
| Пауза | Space или Esc | Кнопка паузы в HUD |
| Старт / рестарт | Enter | Кнопки «Играть» / «Заново» |
| Звук вкл/выкл | M | Кнопка звука в HUD |

Требования:
- **Буфер ввода**: очередь до 3 направлений; каждое нажатие сравнивается с последним в очереди, разворот на 180° игнорируется. Каждый тик из очереди берётся одно направление — быстрые повороты «уголком» не теряются.
- `preventDefault()` на стрелках/пробеле, чтобы страница не скроллилась.
- Клик/тап по канвасу на экранах меню/гейм-овер = старт/рестарт.

## 3. Игровое ядро

- Сетка: **30×20 клеток**, клетка **30px**, канвас **900×600** (CSS масштабирует адаптивно, аспект сохраняется).
- Стены по краям арены — столкновение со стеной = смерть (без прохода сквозь края).
- Скорость: `ticks/сек = 7.5 + 0.5 × (level − 1)`, максимум 14. На боссе скорость замораживается.
- Змейка стартует: 4 сегмента, центр поля, движение вправо.
- Еда:
  - Обычная («пакет данных», пульсирует): **+10 × уровень** очков, +1 сегмент.
  - Бонус («фрагмент кода», жёлтая): после каждых 5 съеденных обычных появляется на 7 сек (последние 2 сек мигает), **+250** очков, +2 сегмента.
- Уровни: каждые 5 съеденных обычных единиц еды → уровень+1: тост «УРОВЕНЬ N», звук `levelup`, рост скорости, смена цвета свечения сетки (палитра-цикл).
- Очки: еда/бонус/заряды (+25)/убийство босса (+250 × номер босса). Рекорд в `localStorage` ключ `cs_best`.
- Смерть: столкновение со стеной, своим телом, телом босса, активным лазером, файрволом. Секвенция: freeze → звук `die` → частицы/тряска/глитч → 1 сек → экран Game Over со счётом и рекордом.
- Жизнь одна. Состояния: `MENU`, `PLAYING`, `BOSS`, `PAUSED`, `GAMEOVER`.

### Отрисовка движения (плавность)
Логика дискретная (тик = сдвиг на клетку). Рендер с интерполяцией: у каждого сегмента `prev{x,y}` и `curr{x,y}`; при отрисовке `t = elapsed / stepInterval` (0..1), позиция = `prev + (curr − prev) × t`. При росте новый хвостовой сегмент получает `prev = curr`. Голова рисуется с ярким свечением и «глазами» по направлению. Тело — скруглённые прямоугольники с градиентом от циановой головы к пурпурному хвосту (lerp по HSL).

## 4. Боссы

Триггер: каждый 3-й уровень (3, 6, 9, …), `bossIndex = level / 3`. Обычная еда во время боя остаётся.

Протокол боя:
- Интро 2 сек: баннер «⚠ БОСС: ИМЯ» (элемент `#boss-banner`), музыка → `boss`.
- У босса **HP = 3 + bossIndex** (4/5/6…). На поле поддерживается **1 «заряд данных»** (зелёный, +25 очков). Съел заряд → боссу 1 урон, заряд респавнится через 2.2 сек.
- Тело босса 2×2 клетки, контакты с ним смертельны. Босс двигается по клеткам, не заходя на змейку/еду/заряды.
- Цикл атак: пауза 2.5 − 0.15 × bossIndex (мин 1.6) сек → случайная атака → пауза → …
- Атаки (с телеграфом — предупреждением):
  - **Лазер**: прицельно по ряду (`head.y`) или колонке (`head.x`) головы. 0.9 сек заряд (клетки ряда мигают красным) → 0.5 сек луч: все клетки ряда/колонки смертельны. Звуки `laser_charge` / `laser_fire`.
  - **Файрвол**: 3 + bossIndex оранжевых блоков в случайных свободных клетках (не ближе 2 клеток к голове, не на еде/зарядах). 0.6 сек «включаются» (не смертельны, мигают) → далее смертельны до конца боя. Суммарный кап 18 блоков.
  - **Рывок** (bossIndex ≥ 2): телеграф ряда босса 0.9 сек → босс пролетает ряд по горизонтали (12 клеток/сек), тело смертельно, у стены останавливается.
- Поражение: взрыв, `boss_die`, +250 × bossIndex очков, все опасности очищаются, баннер «БОСС УНИЧТОЖЕН», музыка → `game`, игра продолжается на том же уровне.
- Имена: `['СТРАЖ СЕТИ', 'ВИРУС-КОРОЛЕВА', 'АЛГОРИТМ ОМЕГА']`, далее `АЛГОРИТМ ОМЕГА mk.N`. Игра бесконечна, боссы циклически усиливаются.

## 5. Звук (Web Audio API, всё синтезируется — без файлов)

Архитектура: `masterGain → compressor → destination`. `AudioContext` создаётся/резюмится по первому пользовательскому жесту (`ensure()`).

Музыка — 16-шаговый секвенсор с lookahead-планировщиком (интервал 25 мс, горизонт 0.12 с):
- `menu` (BPM 84): тёмный пэд (2 расстроенных saw через lowpass) + редкий арп (square + feedback delay 0.23 c).
- `game` (BPM 100): + кик (sine 150→40 Гц) на каждую долю, басовая линия (saw, ля-минор: A1 A1 C2 G1), хэты (шум через highpass) на восьмых.
- `boss` (BPM 128): все слои + дисторшн-бас (waveshaper) + том-филлы, арп быстрее.
- Тональность ля-минор / пентатоника, никакой диссонансной каши — «отличный звук» = чистый микс, каждая партия в своей полосе, короткий референс-громкость: музыка −6 дБ относительно SFX.

SFX (по имени): `eat` (короткий блип вверх), `bonus` (аккорд-чайм), `die` (даун-свип + шумовой краш), `levelup` (арпеджио 4 ноты вверх), `warn` (тревога 2 тона), `laser_charge` (нарастающий писк), `laser_fire` (жёсткий луч: saw+noise), `hit` (удар), `boss_die` (фанфара-взрыв), `click` (UI), `pause` (2 ноты), `start` (свип вверх).

Mute: `M` / кнопка `#mute-btn`, состояние в `localStorage` ключ `cs_muted`.

## 6. Визуальный стиль

- Фон `#04050c`, почти чёрный; панели `rgba(10,14,30,.85)`.
- Неон-палитра: cyan `#00f0ff`, magenta `#ff2bd6`, yellow `#ffe600`, danger `#ff2d55`, orange `#ff7a00`, green `#00ff9d`, линии сетки `rgba(0,240,255,.07)`.
- Акцент сетки по уровням (цикл): `#00f0ff → #ff2bd6 → #ffe600 → #00ff9d → #ff7a00`.
- Шрифт: `'Cascadia Mono', 'Consolas', 'Courier New', monospace`; заголовки — жирные, uppercase, letter-spacing, неоновое свечение (text-shadow).
- CSS-оверлеи поверх канваса: сканлайны (repeating-linear-gradient), виньетка CRT, лёгкий шум.
- Заголовок меню с глитч-анимацией (CSS keyframes: смещение/двоение в magenta/cyan).
- Кнопки: неоновые рамки, hover — заливка + свечение, активный `click`.

## 7. Архитектура и контракты (обязательны к букве)

Порядок скриптов в `index.html`: `js/audio.js`, `js/bosses.js`, `js/fx.js`, `js/ui.js`, `js/game.js`. Все модули — IIFE в strict mode, экспорт в `window.CS` (единый неймспейс). Отступы 2 пробела. Все координаты клеток `{x, y}`, x∈[0..29], y∈[0..19]; ключ клетки — строка `"x,y"`.

### `CS.Audio` (js/audio.js)
```
CS.Audio.ensure()            // создать/резюмнуть AudioContext (по жесту)
CS.Audio.music(mode)         // 'menu' | 'game' | 'boss' | null (стоп)
CS.Audio.sfx(name)           // строка из списка SFX
CS.Audio.setMuted(bool) / getMuted()
```
Вызовы до `ensure()` просто игнорируются (никаких исключений).

### `CS.BossFight` (js/bosses.js)
```
const fight = new CS.BossFight(bossIndex, gridW, gridH, events)
  events = { onDefeated(), onWarn(text), onSfx(name) }
fight.update(dt, snakeCells)   // dt сек; snakeCells = [{x,y},...] голова первая
fight.draw(g, cell)            // g=CanvasRenderingContext2D, cell=размер клетки в px
fight.hazardCells()            // Set<"x,y"> — смертельные клетки этого кадра
fight.collectCharge(x, y)      // true, если там был заряд (урон нанесён)
fight.chargeCells()            // [{x,y}] — позиции зарядов (обычно 1)
fight.active                   // bool
fight.hp / fight.maxHp / fight.name / fight.phase   // phase: 'intro'|'idle'|'telegraph'|'attack'|'dead'
```

### `CS.FX` (js/fx.js)
```
CS.FX.update(dt) / draw(g)              // draw — поверх сцены (частицы, вспышки)
CS.FX.burst(px, py, color, n)           // взрыв частиц в пиксельных координатах
CS.FX.shake(power)                      // тряска экрана
CS.FX.glitch(sec) / CS.FX.flash(color, sec)
```

### `CS.UI` (js/ui.js)
```
CS.UI.show(name)          // 'menu'|'game'|'pause'|'gameover' — переключение экранов (оверлеи)
CS.UI.hud({score, best, level})         // обновить HUD (частичный объект ок)
CS.UI.bossBar(hp, maxHp, show, name)    // show=false скрыть
CS.UI.toast(text)                        // крупная надпись по центру, сама гаснет
CS.UI.banner(text, show)                // #boss-banner
CS.UI.on({start, resume, restart, menu, mute})  // подписка на кнопки
```

### `CS.Game` (js/game.js) — оркестратор
Запускается как `CS.Game.boot()` из DOMContentLoaded. Владеет состоянием, змейкой, едой, уровнями, вводом, главным loop (`requestAnimationFrame`), интеграцией `Audio/Bosses/FX/UI`, рекордом.

## 8. DOM (id обязательны)

```
#game-canvas            канвас 900×600
#hud                    .hud: #score, #best, #level, #btn-pause, #mute-btn
#bossbar (скрыт)        #bossname, #bossfill (div, ширина %)
#toast                  центральная надпись
#boss-banner (скрыт)    баннер предупреждения о боссе
#screen-menu            заголовок, #btn-start, #btn-controls, рекорд
#screen-controls        таблица управления, #btn-controls-back
#screen-pause           #btn-resume, #btn-menu
#screen-gameover        #final-score, #final-best, #btn-restart, #btn-menu2
```
Экраны — `<div class="screen">` поверх канваса (position:absolute), класс `.hidden` прячет. `CS.UI.show('game')` прячет все, показывает игру (т.е. только канвас+HUD).

## 9. Out of scope (не делаем)

- Онлайн-лидерборды, аккаунты, сохранения кроме рекорда и mute.
- Мультиплеер, достижения, настройки графики.
- Нативные аудиофайлы/внешние CDN-ассеты.
- Отдельная мобильная вёрстка (свайпы есть, но десктоп-первичен).

## 10. Плейсхолдеры

Нет — блокирующих неизвестных (оплата/ключи/хостинг) в проекте нет.

---

# РАСШИРЕНИЕ v2 (билеты T8–T10)

## Правило локализации (обязательное, см. AGENTS.md)

Любая видимая игроку строка добавляется **сразу на двух языках** (ru + en) одним коммитом: статичная — через `data-i18n`, динамическая — через `CS.I18N.t()`, ключ — в оба словаря `js/i18n.js`. Хардкод строк в разметке/логике запрещён. Автопроверка: `node check-i18n.js` (код выхода 0 — коммитить можно).

## 11. Пикапы на поле (T8)

Помимо обычной еды и бонуса, на поле по таймеру появляются пикапы (рисуются формами, НЕ квадратами; срок жизни 9 с, последние 2 с мигают):

| Пикап | Форма/цвет | Эффект | Вес |
|---|---|---|---|
| ❤ Жизнь | сердце, #ff2d55 | +1 жизнь (макс 3); при смерти тратится: респавн 3 сегментами в центре, 2 с неуязвимости, счёт/уровень сохраняются | 10% (только если жизней < 3) |
| ⚡ Скачок | молния, #ffe600 | 5 с: скорость ×1.6, очки ×2, радужный след | 15% |
| 🧲 Магнит | подкова, #00f0ff | 6 с: еда/заряды в радиусе 3 клеток собираются автоматически | 10% |
| ❄ Замедление | снежинка, #7de3ff | 5 с: тик ×0.7 (легче уворачиваться) | 15% |
| ☠ Вирус | череп, #ff7a00 | **−50 очков** (не ниже 0) + глитч | 30% |
| ◈ Золотой пакет | ромб с искрой, #ffe600 | +150 очков, без роста | 20% |

- Спавн: каждые 8–14 с один случайный пикап (взвешенно), не во время интро босса, в свободной клетке (правила как у еды). Одновременно на поле ≤ 2 пикапа.
- Смерть с жизнями > 0: расход жизни, «ПЕРЕЗАПУСК СИСТЕМЫ...», респавн, неуязвимость 2 с (мигание змейки, hazards игнорируются).
- HUD: ❤×N рядом со счётом; активные эффекты — маленькие иконки с таймером-полоской в HUD.
- Новые SFX в audio.js: pickup (приятный блип), pickupBad (низкий зловещий), life (тёплый чайм), respawn (перезагрузка).
- Очки могут и падать (вирус, потеря хвоста от босса) — нижняя граница 0.

## 12. Пять новых боссов (T9)

Босс появляется на уровнях, кратных 3: `bossIndex = level/3`. Тип босса: `((bossIndex-1) mod 8)+1`; при `floor((bossIndex-1)/8) > 0` имя получает суффикс `mk.N` и +1 HP за цикл. HP = `min(10, 3 + bossIndex)`.

| # | Имя (RU/EN) | Механики |
|---|---|---|
| 1 | СТРАЖ СЕТИ / NET WARDEN | лазер + файрволы (существующий) |
| 2 | ВИРУС-КОРОЛЕВА / VIRUS QUEEN | + рывок (существующий) |
| 3 | АЛГОРИТМ ОМЕГА / OMEGA ALGORITHM | всё + быстрее (существующий) |
| 4 | ДЕКОМПИЛЯТОР / DECOMPILER | **Рассекающий луч**: телеграф 1 с → горизонтальный ИЛИ вертикальный луч на всю арену. Голова в луче = смерть; **тело в луче = разрез**: сегменты от места пересечения до хвоста отваливаются и остаются на поле «обломками» (пульсирующий циан). Вернись головой на обломки в течение 5 с — каждый поглощённый сегмент восстанавливается (+50 очков). Не успел — сегменты потеряны (−25 очков за потерянный). Иногда добавляет файрвол. |
| 5 | ОХОТНИК-ТУРЕЛЬ / HUNTER TURRET | **Снаряды**: телеграф-прицел (линия) 0.7 с → летящие болты (скорость 9 клеток/с, узкие, длинные), серия из 3; плюс мины (стационарные, мигают 0.8 с, потом смертельны, сами исчезают через 6 с). |
| 6 | ГЛОТ / THE DEVOURER | **Охотится и ест хвост**: постоянно движется к голове змейки (быстрее прочих боссов), при касании СРЕДНЕЙ/ХВОСТОВОЙ части (сегменты ≥ 3) «откусывает» до 3 сегментов за укус (−25 очков за сегмент, минимум 3 сегмента у змейки), касание головы = смерть. Визуально: пасть с зубами, «дожёвывает» после укуса. |
| 7 | КРИОГЕН / CRYOGEN | **Заморозка**: телеграф 1 с → волна по всей арене: скорость змейки ×0.45 на 3 с (змейка бледнеет, ледяные кристаллы), в это время босс ускоряет атаки лазером. Также обычные файрволы. |
| 8 | АДМИН СИСТЕМЫ / SYS ADMIN | Финальный микс: лазер + снаряды + **дроны-антивирусы**: 2 штук, 1×1, медленно преследуют голову, исчезают через 7 с, касание смертельно. |

Новые SFX: shoot (выстрел), freeze (ледяной свист), gulp (чавканье), cut (разрез), drone (жужжание).

Контракт BossFight расширяется событиями (все опциональны, вызываются через try): `onCut(segmentIndex)` (раз в луч), `onTailBite()` (не чаще раза в 1.2 с), `onFreeze(durationSec)`. Снаряды/мины/дроны — внутренности bosses.js, наружу отдаются через hazardCells() как обычно. Заряды-урон, HP-бар, интро, победа — без изменений (§4).

## 13. Таблица лидеров (T10)

- Экран «ЛИДЕРЫ» из меню (кнопка под «Управление»): топ-10 — место, имя, счёт, уровень, дата.
- Хранение: localStorage `cs_board` = JSON [{name, score, level, date}] (макс 10, сортировка по счёту).
- Гейм-овер: если счёт > 0 и входит в топ-10 — поле ввода имени (латиница/кириллица, 1–12 симв., дефолт — последнее имя из `cs_name`) + кнопка «Сохранить»; после — экран таблицы.
- Архитектура: модуль `CS.Leaderboard` c провайдером: `load()` → массив, `submit(entry)` → bool, событие изменения. Локальный провайдер — localStorage; интерфейс совместим с будущим серверным (fetch /api/scores) — глобальная таблица подключится одной заменой провайдера, когда появится хостинг.
- i18n: все надписи экрана ru/en.

## 14. Контейнеры-тайны и хранилище хвоста (T11)

### Контейнеры «?» (7-й тип пикапа)
Перераспределение весов пикапов: virus 25, golden 15, surge 12, slow 12, magnet 8, life 8, **mystery 20**. Внешний вид: пульсирующий куб с «?», свечение мигает cyan↔magenta — содержимое неизвестно до подбора. При подборе: sfx `mystery`, вспышка, бросок кубика на эффект (взвешенно):

| Эффект | Вес | Что делает |
|---|---|---|
| ДЖЕКПОТ | 15% | +500 очков |
| ×2 ОЧКИ | 15% | 10 с все начисления очков удваиваются (чип ×2 в HUD) |
| ТУРБО | 12% | 6 с скорость ×2.0 (сильнее обычного скачка; перемножается с прочими) |
| +1 ЖИЗНЬ | 8% | ❤+1 (при 3 жизнях перебрасывается в ДЖЕКПОТ) |
| РЕВЕРС | 15% | 7 с управление инвертировано (влево↔вправо, вверх↔вниз; клавиши и свайпы), чип ⇄ + тревожный sfx |
| РАСПАД | 15% | змейка распадается: остаётся головная часть из 3 сегментов, из остатков рождаются 2 «сбежавших ядра» (1×1, мечутся, убегают от головы каждую клетку). Поймать каждое головой в течение 8 с → +100; не поймал — растворяются |
| СМЕРТЬ | 10% | мгновенная смерть (жизни спасают как обычно — респавн) |

### Хранилище хвоста (решает вечный рост)
- Каждые 30±8 с (если на поле нет активного) в свободной клетке внутренней зоны (не ближе 2 клеток к стенам) появляется **хранилище** — гексагональный портал с пульсирующим кольцом (green/cyan), срок жизни 10 с (последние 2 с мигает).
- Голова входит в него → «конвертация хвоста»: все сегменты сверх 4 превращаются в очки **+15 × уровень за сегмент**, змейка обрезается до 4 сегментов; sfx `bank` (каскад монет), взрыв частиц по бывшему хвосту, тост «ХВОСТ → +N ОЧКОВ».
- Если длина ≤ 4 — плоские +50 «проценты по вкладу» (тост другой).
- Работает и во время боя с боссом.

Новые SFX: mystery (слот-машина: 3 быстрых случайных тона), reverse (тревога 2 тона вниз), bank (каскад из 5 восходящих монетных блипов), split (рвущийся разряд). i18n: названия/тосты всех эффектов ru/en.

---

# РАСШИРЕНИЕ v3 (билеты T15–T21)

## §15. Telegram-интеграция (T15)
- js/telegram.js → CS.TG: тонкая обёртка над window.Telegram.WebApp (всё через try, вне Telegram — no-op): haptic(type) [error|success|warning|click|heavy], shareScore(score) — открытие t.me/share/url с текстом «Мой рекорд: N · NEON://SNAKE» и ссылкой на мини-апп.
- Вибрации: смерть=error, победа над боссом=success, джекпот=heavy, подбор еды/пикапа=click (не чаще 1 раза в 150мс), урон боссу=click.
- Кнопка «ПОДЕЛИТЬСЯ 📤» на экране гейм-овера (рядом с «Заново»); вне Telegram скрыта.
- game.js/boot: WebApp.ready()+expand() (уже есть — перенести в CS.TG.init()).

## §16. Достижения (T16)
- js/achievements.js → CS.Ach: define/check/unlock/toast, хранение cs_ach (массив id).
- ~12 шт: firstMeal (первая еда), boss1 (первый босс), boss5 (5 боссов за всё время), lvl5, lvl10, score1000, score2500, jackpot (джекпот), jackpot3 (×3 за всё время), cores (обе ядра после распада пойманы), banker (сдать ≥8 сегментов за раз), mystery10 (10 тайн за всё время), survivor (использовать все 3 жизни в одном забеге).
- API: CS.Ach.event(name, value) — события из game.js (meal/bossDown/level/score/jackpot/cores/bank/mystery/respawn).
- Тост специальный (жёлтый, «★ ДОСТИЖЕНИЕ: …»), sfx 'ach' (торжественный 3-тонный).
- Экран «ДОСТИЖЕНИЯ» из меню: сетка, открытые ярко, закрытые — контур + «???» или описание; счётчик «N из 12».

## §17. Скины змейки (T17)
- js/skins.js → CS.Skins: список скинов {id, name(i18n), head/tail HSL, glow, условие разблокировки}: neon (базовый, своб.), ice (голубой — ачв boss1), toxic (кислотно-зелёный — lvl10), magma (оранжево-красный — score2500), gold (золотой — banker), rainbow (переливающийся — boss5), ghost (полупрозрачный белый — mystery10).
- Выбор в меню (кнопка «СКИНЫ» → сетка свотчей с превью-змейкой); активный хранится cs_skin; разблокировки связаны с достижениями (CS.Ach.has).
- game.js drawSnake/segColor: цвета из активного скина; rainbow — hsl по (animTime*60 + i*30)%360.

## §18. Модельки боссов (T18)
- bosses.js: каждому из 8 типов — уникальная модель на канвасе (без ассетов), различимый силуэт и анимация:
  1 СТРАЖ СЕТИ — октаэдр-глаз с вращающимися кольцами; 2 ВИРУС-КОРОЛЕВА — корона из шипов + пульсирующее ядро; 3 ОМЕГА — куб-матрица из 9 мини-кубов; 4 ДЕКОМПИЛЯТОР — циркулярная пила (вращается, при атаке ускоряется); 5 ТУРЕЛЬ — станковое орудие со стволом (поворачивается к цели); 6 ГЛОТ — пасть: раскрытые челюсти с зубами, при укусе захлопываются; 7 КРИОГЕН — кристаллический конгломерат (6-8 граней, мерцание); 8 АДМИН — «монитор»: прямоугольный экран с бегущим «кодом»-строками и антенной.
- Общие принципы: тельеграф-подсветка при атаке, урон — вспышка белым; сохранить существующие цвета-кодировки опасностей.

## §19. Мета-прогресс (T19)
- Валюта: чипы данных = floor(score/100) за забег (тост в конце «+N ЧИПОВ»), банк в cs_chips.
- js/upgrades.js → CS.Upgrades: 4 апгрейда × 3 уровня: «Старт +1 жизнь» (30/90/200), «Магнит +1 радиус» (25/70/150), «Эффекты дольше +40%» (40/100/220), «Пикапы чаще» (интервал −20%) (35/80/180).
- Экран «АПГРЕЙДЫ» из меню: карточки с ценой/уровнем/кнопкой купить (не хватает — приглушено).
- game.js: применяет множители (стартовые жизни, MAGNET_RADIUS, длительность эффектов, интервал спавна пикапов).

## §20. Челлендж дня и сезоны (T20)
- js/daily.js → CS.Daily: сид от даты (YYYY-MM-DD через простой hash) → модификатор дня из 5: «ЗЕРКАЛО» (управление реверс постоянно), «ЛЁД» (постоянная заморозка ×0.6), «СЛИВОК» (еда даёт ×2 очков, но змейка в 2 раза быстрее растёт), «ТЕМНОТА» (видна только область вокруг головы — виньетка-маска), «ОХОТА» (пикапы-тайны в 2 раза чаще, вирус чаще).
- Кнопка «ЧЕЛЛЕНДЖ ДНЯ» в меню: тост с описанием (i18n), забег с флагом; локальный лучший результат дня (cs_daily_YYYY-MM-DD); в меню — «Сегодня: N».
- Сезоны: в облаке — отдельная колонка (SQL у владельца): `alter table scores add column if not exists season text not null default to_char(now(),'YYYY-MM');` — leaderboard.js отправляет season=текущий месяц, читает только текущий сезон (фильтр season=eq.); бейдж «СЕЗОН: МЕСЯЦ ГОД» на экране таблицы; прошлые сезоны не показываются (авторесет).

## §21. Полировка (T21)
- Звук: два ползунка в меню (музыка/эффекты, 0–100), audio.js: setMusicVol/setSfxVol (гейны), хранение cs_mvol/cs_svol.
- Обучение: первые 2 забега (cs_runs) — серия подсказок-тостов: «СТРЕЛКИ/СВАЙП — ДВИЖЕНИЕ» (3с), «ЗЕЛЁНЫЙ РОМБ — УРОН БОССУ» при первом боссе, «ПОДБИРАЙ ❤ ДЛЯ ЗАПАСНОЙ ЖИЗНИ» при первом пикапе жизни.

## §22. Онлайн-дуэль: одна арена, два игрока (T22–T24)

Архитектура: Supabase Realtime (broadcast-каналы `room-XXXX`), хост-авторитарность: браузер создателя считает симуляцию ОБЕИХ змеек, гость шлёт только повороты; хост рассылает снапшоты ~10/с. Клиент supabase-js подгружается ДИНАМИЧЕСКИ только при входе в режим дуэли (офлайн-игра не грузит лишнего — второе задокументированное исключение из «без CDN» в AGENTS.md).

Поле дуэли: площадь ×2 (компьют ~42×28, телефон ~×1.6), адаптивно по аспекту (computeGrid с другим CELLS), скорость тика ×1.15, на поле 2 пакета еды (+1 сегмент, растёт = блокирует пути). Стартовые позиции: противоположные трети поля, движение навстречу.

Правила (матч до 2 побед из 3), СПОСОБЫ ПОБЕДЫ В РАУНДЕ:
1. Классика: соперник врезался в стену или СВОЁ тело — раунд ваш.
2. УКУС: ваша голова врезается в ТЕЛО соперника — вы разрезаете его: сегменты позади места укуса отваливаются в корм (съедобны, +1 сегмент); у соперника осталось <3 сегментов — вы «съели змейку», раунд ваш. Бодание тела соперника — оружие, не смерть (короткая неуязвимость 0.4с при проходе).
3. ЗАКОЛЬЦОВКА: соперник окружён вашим телом/стенами так, что у него нет ни одной свободной клетки — авто-победа раунда с эффектом «ЗАКОЛЬЦОВАН!» (замедление на 1с + баннер).
4. ЛОБ-В-ЛОБ: длинная голова съедает короткую (равные — ничья раунда).
Между раундами 2.5с пауза со счётом раундов.

Жизненный цикл: «ОНЛАЙН-БОЙ» в меню → лобби с кодом комнаты (4 символа) и кнопкой «Позвать в бой» (t.me-ссылка с startapp=room-XXXX через TG.shareScore-подобный шеринг; вне TG — копирование ссылки) → гость заходит по ссылке/коду → отсчёт 3-2-1 → бой → экран результата («ПОБЕДА/ПОРАЖЕНИЕ 2:1», кнопки «РЕВАНШ» (новый раунд-сет в той же комнате) и «ВЫЙТИ»). Отвал соперника (heartbeat 3с) → «СОПЕРНИК ПОКИНУЛ БОЙ» + техническая победа. Имена — из Telegram (fallback — ник лидерборда).

Сетевые события (broadcast JSON): hello{name}, start{}, turn{dir}, state{snakes:[...], food, score, phase}, round{n}, win{side, score}, bye{}, ping/pong. Результаты дуэлей пишутся в таблицу duel_results (id, created_at, winner, loser, rounds, season) — SQL у владельца; экран «ДУЭЛИ» не делаем (статистика в будущем).

## §26. Музыкальные темы (T26): три полноценные композиции

Движок: много-трековый паттерн-плеер: song = {bpm, sections:[{bars, tracks}], loop}. Шаг = 16-я. Треки: pad (аккорды), bass, lead, drums (kick/snare/hat/tom). Все ноты — MIDI-числа, ля-минор.

MENU «Протокол» (84 BPM, 8 тактов, Am9→Fmaj7→Cmaj7→G6 по 2 такта):
- pad: аккорды половинными, тёплый saw-ансамбль через lowpass, медленная атака;
- bass: корни (A2/F2/C3/G2) целыми, sine+triangle;
- lead (вступает с 3-го такта, sine+triangle, длинный delay): фраза A: E5 D5 C5 D5 | C5 A4 G4 A4 (такты 3-4), фраза B: G4 A4 B4 C5 | E5 D5 C5 D5 (5-6), финал: A4 C5 E5 (держать) (7-8);
- drums: мягкий kick на 1 долю, редкий hat.

GAME «Скачок» (100 BPM, прогрессия Am F C G, 8 тактов = секция A + секция B):
- bass: восьмые root-октава (A2 A2 A3 A2 | A2 A3 A2 A3 и транспозиции);
- drums: kick 4-доли, clap на 2/4, восьмые hat с акцентом;
- lead A (по 8-е): | 69 72 76 81 79 76 74 76 | 77 76 74 72 74 76 77 76 | 72 76 79 76 74 72 69 72 | 71 74 79 74 71 69 67 69 |
- lead B (октава ниже + 16-е арп-пробежки в последних 2 тактах).

BOSS «Протокол Омега» (128 BPM, Am G F E, 4 такта):
- bass: искажённый 16-й риф на корне (A2 A2 A3 A2 C3 A2 A3 G#2, транспозиция по аккорду; на E-такте — хроматика E F E D# E);
- stabs: стаккато-квинты на синкопах (root+7 полутона);
- drums: kick 4-доли + синкопы, clap 2/4, 16-е hat, том-филл в конце 4-го такта;
- lead: угроза-мотив нисходящий A5 G5 F5 E5 (16-е → 8-е), повтор с трелью E5-F5-E5.

Правила: переключение режимов — как сейчас (мгновенное, без перезапуска планировщика); mute/vol API не менять; SFX не трогать; отсутствие AudioContext — тихо.
